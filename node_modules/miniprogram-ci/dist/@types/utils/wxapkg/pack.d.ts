/// <reference types="node" />
export interface IFile {
    path: string;
    data: Buffer;
}
export declare function packCore(files: IFile[]): Buffer;
export default function pack(file: string, distPath: string, cwd: string, ignores?: string[]): Promise<Buffer>;
